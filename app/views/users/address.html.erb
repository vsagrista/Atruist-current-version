<head>
  <% content_for :javascript do %>
    <%= javascript_include_tag 'addressSearch.js' %>
  <% end %>
  <script async defer
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyB13TXpc5FZdPqhOX75ny_wVMxSI2H5C9M&callback=initMap">
  </script>
</head>
<div class="wrapper skill-icon-area-container">
  <div class="row">
    <div class="col-md-6">
      <h2 class="name">Welcome, <%= @user.name.capitalize %></h2>
      <% if @user.address.nil? %>
      <p>Add your address on this page <br> Format: street, city, country (no numbers)</p>
      <% else %>
      <p>Your Address: <i><%= @user.address %></i></p>
      <% end %>
    </div>
    <div class="col-md-6">
      <div class="skills-logo-area">
        <img class="skills-icon" src="/assets/map-pointer.jpg">
        <% if @user.address.nil? %>
        <h2><bold>Find your address</bold></h2>
        <% else %>
        <h2><bold>Address Change</bold></h2>
        <% end %>
      </div>
    </div>
  </div>
</div>
<div class="wrapper dashboard-wrapper address">
  <div class="container dashboard-notifiactions-container address-container">
    <h1>Street, city and country (no numbers)
    <img  src="/assets/lens.png">
    <img src="/assets/info-4.png" id="security-signup-icon">
    </h1>
    <p class="security-signup-p text-center">Street, City, Country (NO NUMBER).<br>
      * Home number and/or phone number is sent privately to other users
    </p>
    <hr>
    <div class="text-center">
      <%= form_for @user do |f| %>
      <%= f.text_field :address, id:"address", placeholder: "Search" %>
      <button id="submit" type="button" class="btn btn-primary search-address">
      <img  src="/assets/lens.png">
      </button>
      <%= f.submit "Save", id: "save-address", :disabled => true,
      class: "btn btn-primary save-address" %>
      <% end %>
    </div>
  </div>
  <div id="map" style="width:50%; border-radius: 2%"></div>
</div>
<%= yield :javascript %>
