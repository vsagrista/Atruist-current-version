<nav role="navigation" class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header hidden-sm hidden-md hidden-lg">
            <h1 class="navbar-h1 pull-left"  >Altruist</h1>
            <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle dropdown-btn">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <!-- Collection of nav links and other content for toggling -->
        <div id="navbarCollapse" class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <% if user_signed_in? %>
              <li>
                <%= link_to 'Home', users_dashboard_path, class: "home" %>
              </li>
            <% else %>
              <li>
                <%= link_to 'Log In', new_user_session_path, class: "log-in" %>
              </li>

              <li>
                <%= link_to 'Home', root_path, class: "home" %>
              </li>
            <%end %>
          <% if user_signed_in? %>
            <li>
              <% if current_user.address %>
                <%= link_to "New Search",  users_talentsearch_path %>
              <% else %>
                <%= link_to "Add Address!",  users_address_path %>
              <% end %>
            </li>
            <li>
              <%= link_to "Settings", edit_user_path(current_user) %>
            </li>
            <li>
              <%= link_to 'Sign out', destroy_user_session_path, method: 'get' %>
            </li>
          <% end %>
          </ul>
          <ul class="nav navbar-nav navbar-right hidden-xs">
            <li>
              <div class='img-responsive logo-img-div hide-lg'></div>
            </li>
            <li>
              <h1 class="navbar-h1"> Altruist </h1>
            </li>
          </ul>
      </div>
    </div>
     <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %> error-messages devise">
          <a href="#" data-dismiss="alert" class="close">×</a>
          <ul class="devise-ul">
            <li>
              Attention: <%= value %>
            </li>
          </ul>
       </div>
    <% end %>
</nav>